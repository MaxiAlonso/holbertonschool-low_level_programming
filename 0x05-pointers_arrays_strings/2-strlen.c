#include "main.h"

/**
 * _strlen - returns the length of a string.
 * Return: returns the lenght of a string.
 * @s: pointer to a char
 **/

int _strlen(char *s)
{
	int len;

	len = 0;

	for (len = 0; s[len]; len++)
	{
	}

	return (len);
}
